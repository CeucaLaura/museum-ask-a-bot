import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  gridRootChatlist: {
    height: "calc(100% - 55px)",
    minHeight: 0,
    minWidth: 0,
    width: "100%",
  },
});
