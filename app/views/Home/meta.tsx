import * as React from "react";

import RenderMetaTags from "~/components/RenderMetaTags";

const Meta: React.FC = (): React.ReactElement<any> => (
  <RenderMetaTags description="Page description." />
);

export default Meta;
